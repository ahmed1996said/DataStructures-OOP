//
// Created by Ahmed Al Mahrooqi on 2019-02-26.
//

#ifndef ECE309_SPR19_EMPLOYEEDIRECTORY_H
#define ECE309_SPR19_EMPLOYEEDIRECTORY_H

#include <cstdio>
#include <cstring>
//#include "List.h"

/*
class EmployeeDirectory {
private:

    unsigned int size;
    List* buckets;
    unsigned int hash(const char* key);
    bool find(const char* key, List::iterator& iter);


public:
    EmployeeDirectory(unsigned int s = 0) : size(s), buckets(nullptr) {
        if (size > 0) {
            buckets = new List[size];  // default List is empty
        }
    }

    ~EmployeeDirectory() {
        delete [] buckets;
    }


    bool insert(Employee e);
    const Employee* search(const char* key, Employee& e);
    bool peek(const char* key);
    bool remove(const char* key);
};




*/


#endif //ECE309_SPR19_EMPLOYEEDIRECTORY_H
